VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Set to false to skip magic-link sign-in entirely (matches an "open" RLS
# policy — see supabase/schema.sql). Set to true once the team is invited.
VITE_REQUIRE_AUTH=true
